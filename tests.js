const { assert } = require('chai');
const { it, describe } = require('mocha')

/*
* написать тест, который вызывает
* ssh user:pass@host,
* cd /etc
* ls | grep deb
* get debian_version
* put /path/to/localfile
* TAB filena...
* exit
* */

